= Actions
:toc: right
:imagesdir: actions/images

Actions are the verbs editors use day-to-day on content — creating, editing, moving, publishing, and so on. They are available from three consistent places:

* The *toolbar* at the top of the <<navigator#,Navigator>> and the <<editor#,Editor>>.
* The *context menu* that appears when right-clicking an item in the Navigator.
* *Keyboard shortcuts* — see the <<keyboard-shortcuts#,Keyboard shortcuts>> reference for the full list.

Whether a given action is shown, enabled, or disabled depends on two things: the selection (what content is currently selected) and the user's <<navigator/permissions#,permissions>> on that content.

== In this chapter

<<actions/new#,New>>::
Create a new content item via the *New Content* dialog — either by picking a content type or by uploading a file.

<<actions/edit#,Edit>>::
Open one or more selected items in the Content Editor.

<<actions/duplicate#,Duplicate>>::
Make a copy of the selected content (optionally including its children).

<<actions/move#,Move>>::
Relocate content to a different parent in the tree, preserving children and permissions.

<<actions/sort#,Sort>>::
Configure how child items of the selected content are ordered in the Navigator and on rendered pages.

<<actions/preview#,Preview>>::
Open the rendered page for a site or page content in a new browser tab.

<<actions/publish#,Publish>>::
Push content from the _draft_ branch to _master_ so it becomes available online. Covers the publishing wizard, scheduled publishing, and publishing with dependencies.

<<actions/unpublish#,Unpublish>>::
Remove published content from _master_. Descendants are unpublished along with the selection.

<<actions/delete#,Delete>>::
Remove content from the active tree. Deleted items are moved to the <<archive#,Archive>>, preserving descendants and version history so they can be restored.

== Related reference

*Mark as ready* and the publishing state machine are documented under <<workflow#,Workflow>>. *Request publishing* (the review/approval handoff variant of Publish) is a kind of issue — see <<issues/publish-request#,Publish request>>. *Permanent delete* (from the archive) and restoring archived items are on the <<archive#,Archive>> page. The *Create task* entry point is documented under <<issues#,Issues>>.
