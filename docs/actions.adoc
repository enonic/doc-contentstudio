= Actions
:toc: right
:imagesdir: actions/images

Actions are the verbs editors use day-to-day on content — creating, editing, moving, sorting, and so on. They are available from three consistent places:

* The *toolbar* at the top of the <<navigator#,Content Navigator>> and the <<editor#,Content Editor>>.
* The *context menu* that appears when right-clicking an item in the Navigator.
* *Keyboard shortcuts* — see the <<keyboard-shortcuts#,Keyboard shortcuts>> reference for the full list.

Whether a given action is shown, enabled, or disabled depends on two things: the selection (what content is currently selected) and the user's <<permissions#,permissions>> on that content.

== In this chapter

<<actions/new#,New>>::
Create a new content item via the *New Content* dialog — either by picking a content type or by uploading a file.

<<actions/edit#,Edit>>::
Open one or more selected items in the Content Editor.

<<actions/duplicate#,Duplicate>>::
Make a copy of the selected content (optionally including its children).

<<actions/move#,Move>>::
Relocate content to a different parent in the tree, preserving children and permissions.

<<actions/sort#,Sort>>::
Configure how child items of the selected content are ordered in the Navigator and on rendered pages.

<<actions/preview#,Preview>>::
Open the rendered page for a site or page content in a new browser tab.

== Related reference

Publishing actions (*Publish*, *Unpublish*, *Mark as ready*, *Request publishing*) live with the state machine they operate on — see <<workflow#,Workflow>>. Archiving (*Archive* and permanent *Delete*) is documented on the <<archive#,Archive>> page. Issue-creation entry points (*Create task*) are documented under <<issues#,Issues>>.
