= Workbench
:toc: right
:imagesdir: workbench/images

The *Workbench* is the application shell of Content Studio. It has three parts: a *project bar* across the top, a *menu bar* on the left, and a *main panel* that fills the rest.

image::workbench.png[The application shell with project bar at top and menu bar on the right, width=1024px]

TIP: The <<editor#,Editor>> is not part of the Workbench — it opens in a separate browser tab.

== Project bar

The horizontal bar across the top of the window. It carries application-wide controls that are not specific to any single menuitem:

* The *project context selector* on the left — switches between the projects and layers the current user has access to. Every menuitem (except Settings, see below) inherits the selected project as its context.
* An *open issues* indicator showing the number of unresolved <<issues#,issues>> in the current project, with a dropdown to jump into them.
* The current user's avatar and the XP admin menu on the right.

[[menuitems]]
== Menu bar

The vertical strip on the left lists the available *menuitems*: top-level views contributed by Content Studio or by an installed application. Clicking an icon swaps the <<#main_panel,main panel>> to that menuitem's view.

* *Navigator* — the built-in default, anchored at the top of the menu bar. The content tree, search, and per-content actions. See <<workbench/navigator#,Navigator>>.
* *Settings* — anchored at the bottom-left, set apart from the regular menuitem stack. Unlike the others, Settings is *not* scoped to the currently selected project — it manages all projects and layers. See <<settings#,Settings>>.

Additional menuitems are contributed by installed applications and appear in the stack between Navigator and Settings. The two most common:

* *Archive* — browse, restore, and permanently delete archived content. Contributed by https://market.enonic.com/vendors/enonic/content-studio-plus[Content Studio+]. See <<workbench/archive#,Archive>>.
* *GraphQL Playground* — interactive query console for the Guillotine API. Contributed by https://market.enonic.com/apps/guillotine[Guillotine].

=== Contributing a menuitem

Any installed application can add a menuitem to the menu bar. The icon and view appear when the app is installed in the current project.

See the https://developer.enonic.com/docs/code/stable/extensions/menuitem[menuitems extension reference] for the descriptor format, lifecycle, and implementation details.

== Main panel

The remaining space is the main panel — the surface controlled by the active menuitem. The Navigator splits it into a toolbar, search panel, content tree, <<workbench/preview#,Preview>>, and <<workbench/contextpanel#,Context panel>>; other menuitems arrange the panel differently.

Two of those panels — <<workbench/preview#,Preview>> and <<workbench/contextpanel#,Context panel>> — appear inside multiple menuitem views (Navigator, Archive) and also inside the <<editor#,Editor>>. Their behaviour and extension models are shared across surfaces, which is why they are documented on their own pages.
